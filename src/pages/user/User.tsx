import Details from '../../components/details/Details';
import './user.scss';

const User = () => {
  // Fetch Product Data and send to Details Component

  return (
    <div className="user">
      <Details />
    </div>
  );
};

export default User;
